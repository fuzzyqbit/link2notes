[pytest]
testpaths = tests
markers =
    slow: integration tests that touch the filesystem, qlmanage, or the frozen app (run with -m slow)
addopts = -ra --strict-markers
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
